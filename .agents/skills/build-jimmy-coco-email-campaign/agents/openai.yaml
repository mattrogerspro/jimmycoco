interface:
  display_name: "Build Jimmy Coco Email Campaign"
  short_description: "Build, add to Resend, and commit campaigns"
  default_prompt: "Use $build-jimmy-coco-email-campaign to build, add to Resend, and commit a complete salon campaign for my chosen market."

dependencies:
  tools:
    - type: "mcp"
      value: "resend"
      description: "Inspect and, with explicit approval, manage Resend email resources"
      transport: "streamable_http"
      url: "https://mcp.resend.com/mcp"
