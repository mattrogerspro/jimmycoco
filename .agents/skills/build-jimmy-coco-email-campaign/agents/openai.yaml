interface:
  display_name: "Build Jimmy Coco Email Campaign"
  short_description: "Build campaigns with simple seven-step updates"
  default_prompt: "Use $build-jimmy-coco-email-campaign to build, add to Resend, and commit a complete salon campaign, or label it TEST for a safe learning demo."

dependencies:
  tools:
    - type: "mcp"
      value: "resend"
      description: "Inspect and, with explicit approval, manage Resend email resources"
      transport: "streamable_http"
      url: "https://mcp.resend.com/mcp"
