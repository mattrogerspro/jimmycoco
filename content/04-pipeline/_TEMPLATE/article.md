# <Article title — becomes the `title` field, not an H2 in the body>

> **Answer box.** The number or the answer, in one or two sentences. Nothing
> else. This is what gets pulled into a search snippet and quoted by an
> assistant. Write it last.
>
> Renders as `<blockquote>` — the first child of `.article-body`.

## <The first question the reader is asking>

Body copy. `h2` and `h3` only — the H1 comes from the `title` field.

Arithmetic is visible: `£60 ÷ 28 = £2.14`, never "roughly £2".

Sources named inline where the number is load-bearing, and repeated in
`citations`.

## <Next question>

### <Sub-point>

Tables for anything comparative:

| Thing | Figure | Source |
|---|---|---|
| | | |

## Assumptions

*(Required in any article containing a model.)*

Every figure chosen rather than sourced, with its range, so a reader can
substitute her own.

| Assumption | Used here | Realistic range |
|---|---|---|
| | | |

## What to do about it

Three to five concrete actions.

---

## FAQ

*(Goes into `faq_items`. Questions a reader actually asks — not restated
headings. 40–70 words each, each answer standalone, sources named inline
because the schema block travels without the citations list.)*

**<Question>**
<Answer>

---

## Sources

*(Goes into `citations` as `Label | URL`. Label names the source and its year,
not the claim.)*

1. <Source, year> — <url>

---

*<Standing note, where required. Compliance: general information, current at the
date of publication, not legal advice — requirements vary between local
authorities and insurers, check with your own council and your own broker.
Financial model: these figures are a model, not a forecast — substitute your own
numbers, the assumptions are listed above so you can.>*
