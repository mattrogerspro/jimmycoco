# templates/

The HTML code for this campaign — **one file per email**, named by send order:

```
01-<slug>.html
02-<slug>.html
…
```

Keep each file's copy in step with the matching section in `../sequence.md`. Email-safe HTML only (table-based layout, inline CSS, web-safe fonts) so it renders across clients.
